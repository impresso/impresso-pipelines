# Environment Variables for impresso-pipelines

# Java Home (required for JVM-dependent pipelines: LDA Topics, Solr Normalization)
# On macOS with Apple Silicon, use ARM-native Java installation:
# JAVA_HOME=$(/usr/libexec/java_home -v 23)
# On other systems, point to your Java installation directory
# JAVA_HOME=/path/to/java/home

# Lucene JAR files path (for Solr normalization)
# This should point to the directory containing Lucene JAR files
# Example: export CLASSPATH=$PWD/lucene_jars/*
# CLASSPATH=/path/to/lucene_jars/*

# HuggingFace Hub token (optional, for private models)
# Some pipelines may require authentication for downloading models
# HUGGINGFACE_TOKEN=your_token_here

# AWS credentials (optional, for LDA topics with S3)
# Required if using LDA topics pipeline with S3 storage
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_DEFAULT_REGION=us-east-1

# Test configuration
# PYTEST_ADDOPTS="--verbose --tb=short"
